{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "uls",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/uls",
            "args": ["-l"],
            "cwd": "${workspaceFolder}"
        }
    ]
}